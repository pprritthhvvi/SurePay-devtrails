postgres (The Main Database):

Uses PostgreSQL to permanently store the core relational data for the application (like user accounts, SurePayers, etc.).

It uses a "volume" (postgres_data) so that your data isn't deleted if the container shuts down.


redis (The Cache / Temporary Store):

An ultra-fast, in-memory database.

It is typically used to store frequently accessed data temporarily so the system doesn't have to query the slower Postgres database every single time.


rabbitmq (The Message Broker):

Acts like a post office.

If your backend needs to do something that takes a long time (like sending 1,000 emails or processing a large file), it drops a "message" in RabbitMQ and immediately goes back to serving users.


backend (The Core API):

This is the actual code for the application, built using FastAPI (a modern Python web framework).

It connects to Postgres to save data, Redis to cache data, and RabbitMQ to send background tasks.

It's configured for development with "hot-reloading" (--reload), meaning if you change the Python code, the server updates instantly without needing a restart.


celery_worker (The Background Worker):

This container runs Celery, a Python task queue system.

It constantly listens to RabbitMQ. When the backend drops a time-consuming task into the queue, this Celery worker picks it up and processes it silently in the background so the main API stays fast and responsive.

pgadmin (The Database Dashboard):

A web-based graphical user interface (GUI) for PostgreSQL.

Instead of using the command line to look at your database tables, you can go to localhost:5050 in your browser, log in, and view/edit your database easily.


There were three separate problems happening at the same time causing this confusing issue!

You made a tiny typo 😅: In your message you mentioned using the phone number 976543210. That is only 9 digits and misses the 8! The actual demo credentials set in your backend require 9876543210. Even if the network was perfect, this would legitimately throw an "Invalid credentials" error.
Incorrect Backend IP configured: Your backend was running perfectly and listening for connections, but the IP address hardcoded in mobile-app/src/services/api.js was outdated (10.48.228.75). We checked your machine's current IP address, and it is actually 10.48.228.5! Because of this, the mobile app was trying to talk to a non-existent IP address and timing out.
Flawed Error Handling: You might wonder, if it was a network error, why did it say "login credential failed"? I found a bug in mobile-app/src/screens/LoginScreen.js. The network requests were failing before reaching the server, so the server never returned a status code. However, your frontend's try/catch block was built to display 'Invalid credentials' as a fallback whenever any error occurred. This completely masked network connectivity issues under a misleading message!
How I resolved it permanently:
Updated the IP Address: I fixed api.js to point precisely to your machine's current matching IP (10.48.228.5).
Fixed Error Handling: I updated LoginScreen.js so that if it fails to reach the backend completely, it will pop up a strict "Network Error: Could not connect to the server" alert instead of mistakenly telling you your credentials are bad.
Next Steps:
The issue is now fully resolved! Go ahead and test it on the mobile app, but this time use exactly 9876543210 and demo123. It should let you right in! If your IP changes again in the future, your app will now properly warn you with a Network Error instead of blaming your credentials.